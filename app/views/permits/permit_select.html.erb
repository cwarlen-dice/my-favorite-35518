<%= render "shared/header" %>

<%#= form_with(model: @permit_image, url: check_user_permits_path(current_user.id), method: :post, local: true) do |f| %>
<%= form_with(url: check_user_permits_path(current_user.id), method: :post, local: true) do |f| %>

<div class="permit-images">
  <% @smple_imgs.each_with_index do |item, num| %>
  <div class="permit-checkbox-<%= num %>">
    <% sample_num = 1  %>
    <% item[1].each do |id| %>
    <div class="checkbox-image">
      <label>
        <% if id.is_a?(Integer) %>
        <% sample_num += 1 %>
        <%= f.check_box :ids, {multiple: true, name: 'check_ids[]', class: 'image-check-box'}, id, false %>
        <%= image_tag asset_path("genre/#{item[0]}/#{sample_num}"), class: "parmit-image" %>
        <% else %>
        <%= f.check_box :ids, {multiple: true, name: 'check_ids[]', class: 'image-check-box'}, id.to_i, false %>
        <%= image_tag('#', class: "parmit-image") %>
        <% end %>
      </label>
    </div>
    <% end %>
  </div>
  <% end %>
</div>
<br />
<%= f.submit "submit" %>
<% end %>
