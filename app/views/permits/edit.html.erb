<%= render "shared/header" %>
ジャンル未設定は設定できません<br />
承認画像は最大５つまで選択できます<br />
<%= form_with(model: @permit_image, url: update_user_permits_path(current_user.id), id: 'image-select', method: :patch, local: true) do |f| %>

<%= render 'shared/error_messages', model: f.object %>

<%#= render partial: 'shared/permit_images', locals: {item_genre: @item_genre, f: f} %>
<%= render partial: 'shared/permit_images', locals: {genres: @genres, item_genre: @item_genre, f: f} %>

<%= f.submit "更新", id: 'image-select-sub' %>
<% end %>
